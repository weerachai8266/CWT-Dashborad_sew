# 📖 คู่มือการใช้งาน Automotive Chat LLM

## 🎯 ภาพรวมระบบ
ระบบ Automotive Chat LLM เป็นแอปพลิเคชันสำหรับถาม-ตอบข้อมูลในฐานข้อมูล automotive เป็นภาษาไทย โดยใช้ AI (LLM) ในการวิเคราะห์และตอบคำถาม

---

## 🚀 การเริ่มต้นใช้งาน

### 1. เริ่มระบบ
```bash
cd /home/weerachai
uvicorn chat_llm.app:app --host 0.0.0.0 --port 8001
```

### 2. เข้าใช้งานผ่านเว็บ
- **URL:** http://192.168.0.44:8001
- **API Docs:** http://192.168.0.44:8001/docs

---

## 🌐 การใช้งานผ่านเว็บไซต์

### หน้าหลัก (/)
- **ฟีเจอร์:** อินเทอร์เฟซสำหรับถาม-ตอบแบบ Interactive
- **การใช้งาน:**
  1. พิมพ์คำถามในช่อง "คำถามของคุณ"
  2. กดปุ่ม "ส่งคำถาม" หรือ Ctrl+Enter
  3. รอดูผลลัพธ์

### ปุ่มต่างๆ บนหน้าเว็บ
- 🔵 **ส่งคำถาม:** ส่งคำถามไปประมวลผล
- ⚪ **ล้างข้อมูล:** ล้างคำถามและผลลัพธ์
- ⚪ **ดูรายชื่อตาราง:** แสดงตารางทั้งหมดในระบบ
- ⚪ **รายละเอียดตาราง:** แสดงคำอธิบายของแต่ละตาราง

---

## 🔗 API Endpoints

### 1. ตรวจสอบสถานะระบบ

#### GET /health
```bash
curl http://192.168.0.44:8001/health
```
**ผลลัพธ์:**
```json
{
  "status": "ok",
  "mysql": {
    "host": "192.168.0.44",
    "db": "automotive",
    "allowed_tables": ["sewing_target", "qc_ng", ...]
  },
  "model": "llama3.1"
}
```

### 2. ดูรายชื่อตาราง

#### GET /tables
```bash
curl http://192.168.0.44:8001/tables
```

#### GET /tables/descriptions
```bash
curl http://192.168.0.44:8001/tables/descriptions
```

### 3. ส่งคำถามผ่าน API

#### POST /chat
```bash
curl -X POST http://192.168.0.44:8001/chat \
  -H "Content-Type: application/json" \
  -d '{"question": "มีตารางอะไรบ้างในระบบ?"}'
```

---

## 💬 ตัวอย่างการใช้งาน

### คำถามพื้นฐาน
```
"มีตารางอะไรบ้างในระบบ?"
"แสดงโครงสร้างตาราง sewing_target"
"นับจำนวนแถวในตาราง qc_ng"
```

### คำถามเกี่ยวกับงานเย็บ/ผลิต
```
"เปรียบเทียบจำนวนพนักงานแผน vs จริง"
"สรุป productivity target ประจำเดือน"
"แสดงเป้าหมายการผลิตแยกตาม item"
"หาแผนกที่มีประสิทธิภาพสูงสุด"
```

### คำถามเกี่ยวกับควบคุมคุณภาพ
```
"หาประเภทปัญหา NG ที่เกิดบ่อยที่สุด"
"สรุปจำนวนงาน NG ในสัปดาห์นี้"
"เปรียบเทียบคุณภาพระหว่างแผนก FB กับ FC"
"แนวโน้มปัญหาคุณภาพใน 7 วันที่ผ่านมา"
```

### คำถามแบบวิเคราะห์
```
"หาความสัมพันธ์ระหว่าง productivity กับคุณภาพ"
"สรุปประสิทธิภาพรายแผนกในเดือนกันยายน"
"เปรียบเทียบ actual vs target ย้อนหลัง 30 วัน"
```

---

## 📊 การใช้งานกับ Grafana

### 1. ติดตั้ง Infinity Data Source Plugin

### 2. สร้าง Data Source ใหม่
- **Type:** Infinity
- **URL:** http://192.168.0.44:8001

### 3. สร้าง Query
```json
{
  "type": "json",
  "source": "url",
  "format": "table",
  "url": "/chat",
  "method": "POST",
  "body": {
    "question": "สรุปประสิทธิภาพรายแผนกวันนี้"
  }
}
```

### 4. ตัวอย่าง Dashboard Panels
- **Production Efficiency:** เปรียบเทียบ actual vs target
- **Quality Metrics:** จำนวนและประเภท NG
- **Manpower Analysis:** การใช้พนักงาน actual vs plan

---

## 🔧 การใช้งานกับ HTML/JavaScript

### การเรียก API ด้วย JavaScript
```javascript
// ฟังก์ชันส่งคำถาม
async function askAutomotiveQuestion(question) {
    try {
        const response = await fetch('http://192.168.0.44:8001/chat', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ question: question })
        });
        
        const data = await response.json();
        return data.answer;
    } catch (error) {
        console.error('Error:', error);
        return 'เกิดข้อผิดพลาด: ' + error.message;
    }
}

// ตัวอย่างการใช้งาน
askAutomotiveQuestion('สรุปงาน NG วันนี้')
    .then(answer => console.log(answer));
```

### HTML Form ง่ายๆ
```html
<!DOCTYPE html>
<html>
<head>
    <title>Automotive Query</title>
</head>
<body>
    <form id="queryForm">
        <textarea id="question" placeholder="ใส่คำถามของคุณ..."></textarea>
        <button type="submit">ส่งคำถาม</button>
    </form>
    
    <div id="result"></div>
    
    <script>
        document.getElementById('queryForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const question = document.getElementById('question').value;
            const result = await askAutomotiveQuestion(question);
            document.getElementById('result').innerHTML = `<pre>${result}</pre>`;
        });
    </script>
</body>
</html>
```

---

## 🎯 เคล็ดลับการใช้งาน

### 1. การตั้งคำถาม
- **ชัดเจน:** ระบุช่วงเวลา เช่น "วันนี้", "สัปดาห์นี้", "เดือนกันยายน"
- **เฉพาะเจาะจง:** ระบุชื่อตาราง หรือฟิลด์ที่ต้องการ
- **บริบท:** เพิ่มบริบท เช่น "เปรียบเทียบ", "แนวโน้ม", "รายงานสรุป"

### 2. การใช้ฟิลด์ข้อมูล
- **item:** รุ่น/โมเดลสินค้า
- **qty:** จำนวน
- **created_at:** วันที่บันทึก

### 3. ประเภตตาราง
- **sewing_*:** ข้อมูลการผลิต
- **qc_*:** ข้อมูลคุณภาพ

---

## 🐛 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

#### 1. ไม่สามารถเชื่อมต่อได้
```bash
# ตรวจสอบว่าระบบทำงาน
curl http://192.168.0.44:8001/health
```

#### 2. คำตอบไม่ถูกต้อง
- ตรวจสอบชื่อตาราง: `GET /tables`
- ดูคำอธิบายตาราง: `GET /tables/descriptions`
- ตั้งคำถามใหม่ให้ชัดเจนขึ้น

#### 3. ระบบช้า
- ลดความซับซ้อนของคำถาม
- แบ่งคำถามออกเป็นส่วนย่อย
- ระบุช่วงเวลาที่แคบลง

---

## 📞 การติดต่อ

- **ระบบ:** Automotive Chat LLM v1.0.0
- **เอกสาร:** README.md ในโฟลเดอร์ chat_llm/
- **API Docs:** http://192.168.0.44:8001/docs

---

## 🔄 การอัปเดต

### ตรวจสอบสถานะล่าสุด
```bash
curl http://192.168.0.44:8001/status
```

### รีสตาร์ทระบบ
```bash
pkill -f "uvicorn.*chat_llm"
uvicorn chat_llm.app:app --host 0.0.0.0 --port 8001
```