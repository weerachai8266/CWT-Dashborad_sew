# 📖 คู่มือการใช้งาน Automotive Chat LLM

## 🚀 การเริ่มต้นใช้งาน

### 1. เริ่มระบบ
```bash
# เข้าไปยังไดเรกทอรีโปรเจค
cd /home/weerachai

# รันแอปพลิเคชัน
uvicorn chat_llm.app:app --host 0.0.0.0 --port 8001
```

### 2. เข้าใช้งานผ่านเว็บเบราว์เซอร์
- เปิดเบราว์เซอร์และไปที่: `http://localhost:8001`
- หรือจากคอมพิวเตอร์อื่น: `http://192.168.0.44:8001`

---

## 🖥️ การใช้งานผ่านหน้าเว็บ

### 1. หน้าหลัก
- แสดงสถานะระบบ (ฐานข้อมูล, LLM Agent)
- ช่องสำหรับใส่คำถาม
- ปุ่มต่างๆ สำหรับการทำงาน

### 2. ปุ่มการทำงาน
- **ส่งคำถาม**: ส่งคำถามไปยัง AI Agent
- **ล้างข้อมูล**: ล้างคำถามและผลลัพธ์
- **ดูรายชื่อตาราง**: แสดงตารางที่สามารถสอบถามได้
- **รายละเอียดตาราง**: แสดงคำอธิบายแต่ละตาราง

### 3. ตัวอย่างคำถาม
คลิกที่ตัวอย่างคำถามเพื่อใส่ในช่องคำถามอัตโนมัติ

---

## � หน้าเว็บทดสอบ (Hidden QA Console)

ใช้สำหรับทีมพัฒนาเพื่อทดสอบคำถามแบบเร่งด่วนก่อนเชื่อมต่อกับระบบจริง

- URL: `http://<host>:8001/hidden/chat?token=<HIDDEN_CHAT_TOKEN>`
- ค่าเริ่มต้นของ `HIDDEN_CHAT_TOKEN` คือ `automotive-secret` (เปลี่ยนได้ในไฟล์ `.env` หรือ `config.py`)
- หน้าเว็บนี้ไม่เชื่อมโยงจากเมนูหลัก และแสดงผลเป็นคอนโซลอย่างย่อ
- สามารถล้างซ้ำ/ทดสอบหลายครั้งได้โดยไม่กระทบผู้ใช้งานภายนอก

> ✅ แนะนำให้เปลี่ยนค่า token เมื่อติดตั้งในเครื่องเซิร์ฟเวอร์จริง เพื่อป้องกันการเข้าถึงจากบุคคลภายนอก

---

## �🔧 การใช้งานผ่าน API

### 1. ตรวจสอบสถานะระบบ
```bash
curl http://localhost:8001/health
```

### 2. ส่งคำถาม
```bash
curl -X POST http://localhost:8001/chat \
  -H "Content-Type: application/json" \
  -d '{"question": "สรุปจำนวนงานเย็บในเดือนนี้"}'
```

---

## 💡 ตัวอย่างคำถาม

### งานเย็บและผลิต
- "เปรียบเทียบจำนวนพนักงานแผน vs จริง"
- "แสดงเป้าหมายการผลิตแยกตาม item"
- "สรุป productivity target ของแต่ละแผนก"

### ควบคุมคุณภาพ
- "หาประเภทปัญหาที่เกิดบ่อยที่สุด"
- "สรุปจำนวนงาน NG ในสัปดาห์นี้"
- "เปรียบเทียบคุณภาพระหว่างแผนก"

---

## 🎯 การใช้งานกับ Grafana

```json
{
  "url": "http://192.168.0.44:8001/chat",
  "method": "POST",
  "headers": {
    "Content-Type": "application/json"
  },
  "body": {
    "question": "สรุปจำนวนงานเย็บวันนี้"
  }
}
```

> **Tip:** หากต้องการส่ง token หรือ parameter เพิ่มเติม สามารถเพิ่ม field `params` หรือใส่ token ใน URL ได้ เช่น `http://192.168.0.44:8001/hidden/chat?token=xxx`

### 🔌 ตัวอย่างฝังใน HTML ธรรมดา

```html
<script>
async function askAutomotive(question) {
  const response = await fetch('http://192.168.0.44:8001/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ question })
  });
  const data = await response.json();
  console.log(data.answer);
}

// ตัวอย่างการใช้งาน
askAutomotive('สรุปจำนวนงานเย็บวันนี้');
</script>
```

> สามารถนำโค้ดด้านบนไปต่อยอดในหน้า Dashboard หรือ Web App อื่นๆ ได้ทันที

---

## ⚠️ ข้อควรระวัง

- ระบบอนุญาตเฉพาะคำสั่ง SELECT เท่านั้น
- จำกัดการเข้าถึงตาราง sewing_* และ qc_* เท่านั้น
- ควรตรวจสอบผลลัพธ์ก่อนนำไปใช้งานจริง

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: ถามแล้วไม่ตอบ

#### 1. ตรวจสอบ Ollama Service
```bash
# ตรวจสอบว่า Ollama ทำงานหรือไม่
ollama list

# ทดสอบ Ollama
ollama run llama3.1 "สวัสดี"
```

#### 2. ตรวจสอบสถานะระบบ
```bash
curl http://localhost:8001/health
curl http://localhost:8001/status
```

#### 3. รีสตาร์ท Ollama (ถ้าค้าง)
```bash
sudo systemctl restart ollama
# หรือ
pkill -f ollama
ollama serve
```

### รีสตาร์ทระบบ
```bash
pkill -f "uvicorn.*chat_llm"
uvicorn chat_llm.app:app --host 0.0.0.0 --port 8001
```

### ตรวจสอบ Log
```bash
# ดู log ของแอปพลิเคชัน
tail -f /var/log/uvicorn.log

# หรือรันในโหมด verbose
uvicorn chat_llm.app:app --host 0.0.0.0 --port 8001 --log-level debug
```

---

*คู่มือการใช้งาน - อัปเดตล่าสุด: 30 กันยายน 2025*