# 🔧 แก้ไขปัญหา SQL Error: Unknown column 'item' in 'field list'

## 🐛 ปัญหาที่พบ

```
❌ Error: (mysql.connector.errors.ProgrammingError) 1054 (42S22): 
Unknown column 'item' in 'field list' 
[SQL: SELECT SUM(CASE WHEN item = 'fc' THEN qty ELSE 0 END) AS fc, ...]
```

**สาเหตุ:**
AI เข้าใจผิดว่า `item` คือชื่อแผนก (FB, FC, RB, RC) และพยายาม filter ด้วย `WHERE item = 'fc'`

**ความจริง:**
- `item` = รุ่นสินค้า/โมเดล (เช่น RJ08, RJ10, G-LEATHER)
- แผนก = ชื่อตาราง (sewing_fb, sewing_fc, sewing_rb, sewing_rc)
- แต่ละตารางแยกแผนกอยู่แล้ว ไม่มี column ชื่อแผนก

---

## ✅ วิธีแก้ไข

### 1. อัพเดท SYSTEM_HINT ใน `llm/config.py`

**เพิ่มความชัดเจน:**

```python
# เพิ่มคำอธิบายชื่อแผนก
🏭 ชื่อแผนก (Department) ในโรงงาน:
- FB = Front Body (แผนกเย็บส่วนหน้าตัวถัง)
- FC = Front Cushion (แผนกเย็บเบาะหน้า)
- RB = Rear Body (แผนกเย็บส่วนหลังตัวถัง)
- RC = Rear Cushion (แผนกเย็บเบาะหลัง)
- 3RD = Third Party (งานจ้างเหมาภายนอก)
- SUB = Subcomponent (งานเย็บอะไหล่)

# เพิ่มตัวอย่าง SQL ที่ชัดเจน
📝 ตัวอย่าง SQL ที่ถูกต้อง:

❓ คำถาม: "แสดงกราฟจำนวนงานเย็บวันนี้แยกตามแผนก"
✅ ถูก: ต้อง query แต่ละตาราง (sewing_fb, sewing_fc, ฯลฯ) แล้ว UNION
  SELECT 'FB' as department, SUM(qty) as total 
  FROM sewing_fb 
  WHERE DATE(created_at) = CURDATE()
  UNION ALL
  SELECT 'FC', SUM(qty) FROM sewing_fc WHERE DATE(created_at) = CURDATE()
  UNION ALL
  SELECT 'RB', SUM(qty) FROM sewing_rb WHERE DATE(created_at) = CURDATE()
  ...
```

**เน้นย้ำความแตกต่าง:**

```python
⚠️ สำคัญ! โครงสร้างตารางการผลิต:
- sewing_fb: ข้อมูลผลิตแผนก FB - Columns: id, item, qty, status, created_at
- sewing_fc: ข้อมูลผลิตแผนก FC - Columns: id, item, qty, status, created_at
...

🔑 หมายเหตุสำคัญ:
  * แต่ละตาราง (sewing_fb, sewing_fc, ฯลฯ) แยกแผนกอยู่แล้ว
  * item = รุ่นสินค้า/โมเดล (เช่น RJ08, RJ10) ไม่ใช่ชื่อแผนก!
  * ถ้าต้องการข้อมูลแผนก FB ให้ query จาก sewing_fb เท่านั้น
  * ถ้าต้องการแยกตามแผนก ให้ query แต่ละตารางแยก (UNION ALL)
```

---

### 2. รีสตาร์ท LLM Server

```bash
./start_llm.sh restart
```

---

### 3. ทดสอบ

**คำถาม:**
```
แสดงกราฟจำนวนงานเย็บวันนี้แยกตามแผนก FB FC RB RC
```

**ผลลัพธ์:** ✅ สำเร็จ!
```json
{
    "answer": "วันนี้มีงานเย็บรวมกัน 389 ชิ้น โดยแยกตามแผนกดังนี้:\n\n* แผนก FB มีงานเย็บ 235 ชิ้น\n* แผนก FC มีงานเย็บ 77 ชิ้น\n* แผนก RB มีงานเย็บ 58 ชิ้น\n* แผนก RC มีงานเย็บ 914 ชิ้น",
    "chart": {
        "type": "bar",
        "title": "จำนวนงานเย็บวันนี้แยกตามแผนก",
        "x_label": "แผนก",
        "y_label": "จำนวนงานเย็บ",
        "data": [
            {"label": "RC", "value": 914.0},
            {"label": "FB", "value": 235.0},
            {"label": "FC", "value": 77.0},
            {"label": "RB", "value": 58.0}
        ]
    }
}
```

---

## 📊 SQL ที่ถูกต้อง

### ก่อนแก้ไข (❌ ผิด)

```sql
-- AI พยายามใช้ item เป็น filter แผนก
SELECT 
  SUM(CASE WHEN item = 'fc' THEN qty ELSE 0 END) AS fc,
  SUM(CASE WHEN item = 'fb' THEN qty ELSE 0 END) AS fb
FROM sewing_fc;  -- ❌ ผิด! item ไม่ใช่ชื่อแผนก
```

### หลังแก้ไข (✅ ถูก)

```sql
-- Query แต่ละตารางแยก แล้ว UNION
SELECT 'FB' as department, SUM(qty) as total 
FROM sewing_fb 
WHERE DATE(created_at) = CURDATE()

UNION ALL

SELECT 'FC', SUM(qty) 
FROM sewing_fc 
WHERE DATE(created_at) = CURDATE()

UNION ALL

SELECT 'RB', SUM(qty) 
FROM sewing_rb 
WHERE DATE(created_at) = CURDATE()

UNION ALL

SELECT 'RC', SUM(qty) 
FROM sewing_rc 
WHERE DATE(created_at) = CURDATE();
```

---

## 📝 ไฟล์ที่แก้ไข

```
✅ llm/config.py  - อัพเดท SYSTEM_HINT
```

**การเปลี่ยนแปลง:**
1. เพิ่มคำอธิบายชื่อแผนก (FB, FC, RB, RC)
2. เน้นย้ำว่า `item` คือรุ่นสินค้า ไม่ใช่ชื่อแผนก
3. เพิ่มตัวอย่าง SQL ที่ถูกต้องสำหรับแต่ละกรณี
4. เพิ่มคำอธิบายโครงสร้างตารางแบบละเอียด

---

## 💡 เคล็ดลับการใช้งาน

### ✅ คำถามที่ชัดเจน

```
✅ "แสดงกราฟจำนวนงานเย็บวันนี้แยกตามแผนก FB FC RB RC"
✅ "สรุปผลผลิตแผนก FB และ FC วันนี้"
✅ "เปรียบเทียบจำนวนงานเย็บแต่ละแผนก"
```

### 📌 ระบุแผนกที่ต้องการ

```
✅ "จำนวนงานเย็บแผนก FB วันนี้"
✅ "แสดงข้อมูลแผนก FC และ RC"
✅ "เฉพาะแผนก 3RD"
```

### 📊 ถ้าต้องการแยกตามรุ่นสินค้า

```
✅ "แสดงกราฟแยกตามรุ่นสินค้าในแผนก FB"
✅ "รุ่นสินค้าใดที่ผลิตมากที่สุดวันนี้"
✅ "แยกตาม item ในแผนก FC"
```

---

## 🔍 การ Debug

### ดู SQL ที่ AI สร้าง

```bash
tail -f llm/llm.log | grep "SQL ที่สร้างขึ้น"
```

### ทดสอบ SQL โดยตรง

```bash
mysql -h 192.168.0.44 -u user -puser automotive << EOF
SELECT 'FB' as department, SUM(qty) as total 
FROM sewing_fb 
WHERE DATE(created_at) = CURDATE()
UNION ALL
SELECT 'FC', SUM(qty) FROM sewing_fc WHERE DATE(created_at) = CURDATE();
EOF
```

---

## 🎯 สรุป

### ปัญหา:
- AI เข้าใจผิดว่า `item` คือชื่อแผนก
- พยายาม filter ด้วย `WHERE item = 'fc'`
- เกิด SQL Error: Unknown column

### การแก้ไข:
- ✅ อัพเดท SYSTEM_HINT ให้ชัดเจน
- ✅ เพิ่มตัวอย่าง SQL ที่ถูกต้อง
- ✅ เน้นความแตกต่างระหว่าง item และ department

### ผลลัพธ์:
- ✅ AI สร้าง SQL ถูกต้อง (UNION ALL)
- ✅ แยกตามแผนกได้ถูกต้อง
- ✅ สร้างกราฟได้สำเร็จ

---

## 🎉 สำเร็จแล้ว!

**ระบบพร้อมใช้งาน:**
- ✅ แก้ไข SQL Error แล้ว
- ✅ แยกตามแผนกถูกต้อง
- ✅ สร้างกราฟได้
- ✅ ทดสอบผ่าน

**ลองใช้งานได้เลย! 🚀**

---

## 📞 หากพบปัญหาอื่น

1. **ดู logs:**
   ```bash
   ./start_llm.sh logs
   ```

2. **รีสตาร์ท:**
   ```bash
   ./start_llm.sh restart
   ```

3. **อ่านเอกสาร:**
   - [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
   - [CHART_GUIDE.md](CHART_GUIDE.md)

**ขอให้ใช้งานอย่างมีความสุข! 😊**
