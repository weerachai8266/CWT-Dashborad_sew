# 🔧 คู่มือแก้ไขปัญหา AI Assistant

## ปัญหาที่พบบ่อยและวิธีแก้ไข

### 1. ❌ "การเชื่อมต่อกับ LLM Server ล้มเหลว: Operation timed out"

**สาเหตุ**: คำถามใช้เวลาประมวลผลนานเกินไป (มากกว่า 60 วินาที)

**วิธีแก้ไข**: 
✅ **แก้ไขแล้ว!** ระบบได้ปรับ timeout เป็น 3 นาที (180 วินาที)

**เคล็ดลับการถามคำถาม**:
```
❌ ไม่ควร: "วิเคราะห์ข้อมูลการผลิตทั้งหมดในปีนี้"
✅ ควร: "สรุปจำนวนงานเย็บวันนี้แยกตามแผนก"

❌ ไม่ควร: "หาข้อมูลทุกอย่างที่เกี่ยวกับคุณภาพ"
✅ ควร: "หา NG ที่พบบ่อยที่สุด 10 อันดับ"
```

---

### 2. 🐌 AI ตอบช้ามาก

**สาเหตุ**: 
- คำถามซับซ้อน
- Ollama Server ช้า
- ข้อมูลในฐานข้อมูลมาก

**วิธีแก้ไข**:

#### วิธีที่ 1: สลับไปใช้ Gemini (แนะนำ)
```bash
# แก้ไข llm/.env
LLM_PROVIDER=gemini

# รีสตาร์ท
./start_llm.sh restart
```

#### วิธีที่ 2: เพิ่มประสิทธิภาพ Ollama
```bash
# ใช้โมเดลที่เล็กกว่า
cd llm
nano .env

# เปลี่ยนเป็น
OLLAMA_MODEL=gemma:2b  # เล็กกว่า gemma3:4b
```

#### วิธีที่ 3: จำกัดข้อมูล
```
✅ ถามแบบจำกัดวัน: "สรุปข้อมูลวันนี้"
✅ ถามแบบจำกัดแผนก: "แสดงข้อมูล FB เท่านั้น"
✅ ใช้ LIMIT: "แสดง 10 รายการแรก"
```

---

### 3. 🔴 "ระบบ AI ไม่พร้อมใช้งาน"

**สาเหตุ**: LLM Server หยุดทำงาน

**วิธีตรวจสอบ**:
```bash
./start_llm.sh status
```

**วิธีแก้ไข**:
```bash
# รีสตาร์ทระบบ
./start_llm.sh restart

# ถ้ายังไม่ได้ ดู logs
./start_llm.sh logs

# หรือ
tail -f llm/llm.log
```

---

### 4. 💬 Chat Widget ไม่แสดง

**วิธีแก้ไข**:

1. **ตรวจสอบ Console (F12)**:
   - กด F12 → Console tab
   - ดู error messages

2. **ตรวจสอบไฟล์**:
   ```bash
   ls -la api/chat_llm.php
   ls -la script/script.js
   ```

3. **ล้าง Cache**:
   - กด Ctrl+F5 (Windows/Linux)
   - กด Cmd+Shift+R (Mac)

---

### 5. ⚠️ "LLM Provider is set to 'gemini' but API key is empty"

**วิธีแก้ไข**:

```bash
# แก้ไข llm/.env
nano llm/.env

# เพิ่ม API key
GEMINI_API_KEY=your-actual-api-key-here

# รีสตาร์ท
./start_llm.sh restart
```

**หรือสลับกลับไปใช้ Ollama**:
```bash
# แก้ไข llm/.env
LLM_PROVIDER=ollama

# รีสตาร์ท
./start_llm.sh restart
```

---

### 6. 🔌 "Cannot connect to Ollama Server"

**วิธีแก้ไข**:

1. **ตรวจสอบ Ollama Server**:
   ```bash
   curl http://192.168.0.9:11434/api/tags
   ```

2. **ถ้า Ollama ไม่ทำงาน**:
   - ติดต่อผู้ดูแล Ollama Server
   - หรือสลับไปใช้ Gemini (ดูวิธีที่ 5)

---

### 7. 📊 กราฟไม่แสดง

**สาเหตุ**: AI ไม่ได้ส่งข้อมูลกราฟมา

**วิธีแก้ไข**:
```
✅ ถามให้ชัดเจนว่าต้องการกราฟ:
- "แสดงกราฟจำนวนงานเย็บแยกตามแผนก"
- "สร้างกราฟแนวโน้ม NG ย้อนหลัง 7 วัน"
```

---

### 8. 🗄️ "Database connection failed"

**วิธีแก้ไข**:

1. **ตรวจสอบ config**:
   ```bash
   nano llm/.env
   ```

2. **ทดสอบการเชื่อมต่อ**:
   ```bash
   mysql -h 192.168.0.44 -u user -p automotive
   ```

3. **ตรวจสอบว่าตรงกับ config/db.php**:
   ```bash
   cat config/db.php
   ```

---

## 📈 ตัวอย่างการใช้งานที่ดี

### คำถามที่ได้ผลดี ✅

```
✅ "สรุปจำนวนงานเย็บวันนี้"
✅ "หา NG ที่พบบ่อยที่สุด 10 อันดับ"
✅ "เปรียบเทียบจำนวนพนักงาน FB กับ FC"
✅ "แสดงกราฟการผลิตสัปดาห์นี้"
✅ "มีตารางอะไรบ้างในระบบ?"
```

### คำถามที่อาจช้า ⚠️

```
⚠️ "วิเคราะห์ข้อมูลการผลิตทั้งปี"
⚠️ "แสดงข้อมูลทุกอย่างที่มี"
⚠️ "สรุปข้อมูล 6 เดือนย้อนหลัง"
⚠️ "เปรียบเทียบทุกแผนกทุกเดือน"
```

---

## 🔍 การตรวจสอบระบบ

### ตรวจสอบสถานะครบวงจร

```bash
# 1. LLM Server
./start_llm.sh status

# 2. LLM Health
curl http://localhost:8000/health

# 3. PHP API
curl http://your-server/sewing/api/chat_llm.php

# 4. Database
mysql -h 192.168.0.44 -u user -p -e "USE automotive; SHOW TABLES;"

# 5. Ollama (ถ้าใช้)
curl http://192.168.0.9:11434/api/tags
```

---

## 📞 ติดต่อสนับสนุน

หากแก้ไขไม่ได้ กรุณา:

1. **เก็บ Logs**:
   ```bash
   ./start_llm.sh logs > llm_error.log
   ```

2. **ดู Browser Console** (F12):
   - Screenshot error messages

3. **บันทึกคำถามที่ถาม**:
   - คำถามที่ใช้
   - เวลาที่เกิดปัญหา

4. **ติดต่อทีมพัฒนา** พร้อมข้อมูลข้างต้น

---

## ⚡ Quick Fixes

```bash
# รีสตาร์ททุกอย่าง
./start_llm.sh restart
sudo systemctl restart apache2  # หรือ nginx

# ล้าง cache
rm -rf llm/__pycache__
rm -rf llm/*.pyc

# ติดตั้ง dependencies ใหม่
cd llm
pip install -r requirements.txt --force-reinstall

# ตรวจสอบ disk space
df -h
```

---

## 🎯 สรุป

**ปัญหาที่พบบ่อยที่สุด**: Timeout
**วิธีแก้**: ระบบได้ปรับ timeout เป็น 3 นาที แล้ว ✅

**Tips สำหรับประสบการณ์ที่ดี**:
1. ✅ ถามคำถามสั้นและชัดเจน
2. ✅ จำกัดช่วงเวลา (วันนี้, สัปดาห์นี้)
3. ✅ ระบุแผนกหรือตารางที่ต้องการ
4. ✅ ใช้ LIMIT เมื่อต้องการข้อมูลจำนวนมาก
5. ✅ ถ้าใช้ Ollama ช้า → สลับไป Gemini

**ขอให้ใช้งานอย่างมีความสุข! 🚀**
