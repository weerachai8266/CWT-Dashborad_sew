# CWT Production Dashboard

ระบบติดตามและวิเคราะห์ข้อมูลการผลิตสำหรับโรงงานเย็บผ้า พร้อมระบบ AI Assistant สำหรับถาม-ตอบข้อมูล

## � ฟีเจอร์หลัก

### 1. 📊 Production Dashboard
- แสดงข้อมูลการผลิตแบบ Real-time
- Toggle Switch สลับระหว่าง "ชิ้น" และ "เปอร์เซ็น"
- Color Coding System สำหรับประสิทธิภาพ
  - 🔴 **แดง (0-79%)**: ต่ำกว่าเป้าหมายมาก
  - 🟡 **เหลือง (80-94%)**: ใกล้เป้าหมาย  
  - 🟢 **เขียว (95-104%)**: ตามเป้าหมาย
  - 🔵 **น้ำเงิน (105%+)**: เกินเป้าหมาย

### 2. ✅ Quality Control
- ติดตามข้อมูลคุณภาพสินค้า
- วิเคราะห์ NG (Not Good) แยกตามประเภท
- สรุปปัญหาที่พบบ่อย

### 3. � Performance Analysis
- วิเคราะห์ประสิทธิภาพการผลิต
- เปรียบเทียบแผนกต่างๆ
- Export ข้อมูลเป็น Excel

### 4. 🤖 AI Assistant (ใหม่!)
- **ถาม-ตอบข้อมูล**: ถามคำถามเป็นภาษาไทยเกี่ยวกับข้อมูลการผลิต
- **แสดงกราฟอัตโนมัติ**: สร้างกราฟจากคำตอบที่ได้
- **วิเคราะห์ข้อมูล**: สรุปและวิเคราะห์ข้อมูลด้วย AI
- **รองรับ 2 โมเดล**: Ollama (Local) หรือ Gemini (Cloud)

---

## 📋 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น
- PHP 7.4+
- MySQL/MariaDB
- Python 3.8+ (สำหรับ AI Assistant)
- Ollama (ถ้าใช้ Local LLM) หรือ Gemini API Key

### 1. ติดตั้งระบบหลัก

```bash
# Clone repository
git clone https://github.com/weerachai8266/CWT-monitor_show.git
cd CWT-monitor_show

# ตั้งค่าฐานข้อมูล
# แก้ไขไฟล์ config/db.php ให้ตรงกับฐานข้อมูลของคุณ

# Import database
mysql -u user -p automotive < sql/sql.sql
```

### 2. ติดตั้ง AI Assistant

```bash
# ติดตั้ง Python dependencies
cd llm
pip install -r requirements.txt

# ตั้งค่า .env
cp .env.example .env
nano .env  # แก้ไขค่าต่างๆ ตามต้องการ
```

**ตัวอย่างไฟล์ `.env`:**
```env
# ฐานข้อมูล
MYSQL_HOST=192.168.0.44
MYSQL_PORT=3306
MYSQL_USER=user
MYSQL_PASS=user
MYSQL_DB=automotive

# เลือก LLM Provider
LLM_PROVIDER=ollama  # หรือ gemini

# Ollama (Local)
OLLAMA_HOST=http://192.168.0.9:11434
OLLAMA_MODEL=gemma3:4b
OLLAMA_TEMPERATURE=0.5

# Gemini (Cloud)
GEMINI_API_KEY=your-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp
GEMINI_TEMPERATURE=0.3
```

### 3. เริ่มใช้งาน

#### เริ่ม AI Assistant

```bash
# จากโฟลเดอร์หลักของโปรเจกต์
./start_llm.sh start

# คำสั่งอื่นๆ
./start_llm.sh stop     # หยุดการทำงาน
./start_llm.sh restart  # รีสตาร์ท
./start_llm.sh status   # ตรวจสอบสถานะ
./start_llm.sh logs     # แสดง log
```

#### เปิดใช้งานระบบ

1. เปิด web browser ไปที่ `http://your-server/sewing/index.html`
2. คลิกปุ่ม AI Assistant (ไอคอนหุ่นยนต์) ที่มุมขวาล่าง
3. เริ่มถามคำถามได้เลย!

---

## 🎯 การใช้งาน AI Assistant

### ตัวอย่างคำถาม

#### คำถามทั่วไป
```
- มีตารางอะไรบ้างในระบบ?
- แสดงข้อมูลในตาราง sewing_target 5 แถวแรก
```

#### งานเย็บ/ผลิต
```
- สรุปจำนวนงานเย็บวันนี้แยกตามแผนก
- เปรียบเทียบจำนวนพนักงานแผน vs จริง
- แสดงเป้าหมายการผลิตจาก sewing_target แยกตาม item
- สรุป productivity target จากตาราง sewing_productivity_plan
```

#### ควบคุมคุณภาพ
```
- หาประเภทปัญหาจาก qc_issue ที่เกิดบ่อยที่สุด 10 อันดับ
- สรุปจำนวนงาน NG จาก qc_ng ในเดือนนี้
- เปรียบเทียบคุณภาพระหว่างแผนก FB, FC, RB, RC
- แสดงกราฟแนวโน้ม NG ในสัปดาห์นี้
```

#### วิเคราะห์ข้อมูล
```
- วิเคราะห์ประสิทธิภาพการผลิตรายสัปดาห์
- หาแผนกที่มีปัญหาคุณภาพมากที่สุด
- สรุปผลผลิตเฉลี่ยต่อวันในเดือนที่แล้ว
```

#### 📊 สร้างกราฟอัตโนมัติ (ใหม่!)
```
- แสดงกราฟจำนวนงานเย็บวันนี้แยกตามแผนก
- สร้างกราฟเปรียบเทียบผลผลิต FB vs FC
- กราฟแสดง NG ที่พบบ่อยที่สุด 10 อันดับ
- แสดงกราฟแนวโน้มของเสียย้อนหลัง 7 วัน
- สร้างกราฟสัดส่วนประเภทปัญหาคุณภาพ
```

**Tips สำหรับกราฟ:**
- เริ่มคำถามด้วย "แสดงกราฟ" หรือ "สร้างกราฟ"
- ระบุการจำแนกข้อมูล (แยกตามแผนก, แยกตามวัน)
- จำกัดช่วงเวลา (วันนี้, สัปดาห์นี้, TOP 10)
- ดูคู่มือเพิ่มเติม: [CHART_GUIDE.md](CHART_GUIDE.md)

### ฟีเจอร์พิเศษ
- **📊 กราฟอัตโนมัติ**: AI จะสร้างกราฟให้อัตโนมัติ (Bar, Line, Pie, Doughnut)
- **📅 บริบทวันที่**: เข้าใจคำว่า "วันนี้", "เดือนนี้", "สัปดาห์นี้"
- **🇹🇭 ภาษาไทย**: รองรับคำถามและคำตอบเป็นภาษาไทยเต็มรูปแบบ
- **⚡ Real-time**: ตอบคำถามจากข้อมูลล่าสุดในฐานข้อมูล

---

## 🔧 ไฟล์สำคัญ

### Frontend
- `index.html` - หน้าหลักของระบบ
- `css/style.css` - CSS รวม UI ของ AI Chat Widget
- `script/script.js` - JavaScript รวมฟังก์ชัน AI Chat

### Backend
- `api/chat_llm.php` - API Proxy สำหรับเชื่อมต่อกับ LLM Server
- `api/get_production_data.php` - API ดึงข้อมูลการผลิต
- `api/get_performance.php` - API ดึงข้อมูลประสิทธิภาพ
- `config/db.php` - การตั้งค่าฐานข้อมูล

### AI System
- `llm/app.py` - FastAPI application
- `llm/llm_agent.py` - LLM Agent และ Langchain
- `llm/database.py` - จัดการฐานข้อมูล
- `llm/config.py` - การตั้งค่าระบบ LLM
- `llm/.env` - Environment variables
- `start_llm.sh` - สคริปต์จัดการ LLM Server

---

## 🛠️ การแก้ไขปัญหา

### AI Assistant ไม่ตอบสนอง

1. **ตรวจสอบว่า LLM Server ทำงานอยู่:**
   ```bash
   ./start_llm.sh status
   ```

2. **ตรวจสอบ logs:**
   ```bash
   ./start_llm.sh logs
   # หรือ
   tail -f llm/llm.log
   ```

3. **ทดสอบ API โดยตรง:**
   ```bash
   curl http://localhost:8000/health
   ```

### Ollama ไม่ทำงาน

1. **ตรวจสอบว่า Ollama Server online:**
   ```bash
   curl http://192.168.0.9:11434/api/tags
   ```

2. **สลับไปใช้ Gemini:**
   ```bash
   # แก้ไขใน llm/.env
   LLM_PROVIDER=gemini
   
   # รีสตาร์ท
   ./start_llm.sh restart
   ```

### ปัญหาการเชื่อมต่อฐานข้อมูล

1. **ตรวจสอบ config/db.php:**
   ```php
   $host = '192.168.0.44';
   $user = 'user';
   $pass = 'user';
   $db = 'automotive';
   ```

2. **ตรวจสอบ llm/.env:**
   ```env
   MYSQL_HOST=192.168.0.44
   MYSQL_USER=user
   MYSQL_PASS=user
   ```

---

## 🎨 การปรับแต่ง

### เปลี่ยนสี AI Chat Widget

แก้ไขใน `css/style.css`:
```css
.chat-toggle-btn {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### เปลี่ยนตำแหน่ง Chat Button

แก้ไขใน `css/style.css`:
```css
.ai-chat-widget {
    bottom: 30px;  /* เปลี่ยนระยะจากด้านล่าง */
    right: 30px;   /* เปลี่ยนระยะจากด้านขวา */
}
```

### เพิ่มคำถามตัวอย่าง

แก้ไขใน `index.html` ที่ส่วน welcome message:
```html
<ul class="mb-0 mt-2">
    <li>คำถามใหม่ของคุณ</li>
</ul>
```

---

## 📚 เอกสารเพิ่มเติม

- [LLM System Documentation](llm/README.md) - คู่มือระบบ AI โดยละเอียด
- [API Documentation](llm/USAGE_GUIDE.md) - วิธีใช้งาน API
- [Model Information](llm/MODEL.md) - ข้อมูลเกี่ยวกับ AI Models

---

## 🔄 การอัพเดท

### อัพเดทโมเดล Ollama

```bash
ollama pull gemma3:4b
# หรือโมเดลอื่นๆ
ollama pull llama3.1
ollama pull codellama
```

### อัพเดท Python Dependencies

```bash
cd llm
pip install -r requirements.txt --upgrade
```

---

## 🤝 การพัฒนาต่อ

### เพิ่มตารางใหม่ใน AI System

แก้ไขใน `llm/database.py`:
```python
def get_default_allowed_tables() -> List[str]:
    return [
        'ชื่อตารางใหม่',
        # ...
    ]
```

### ปรับแต่ง AI Prompt

แก้ไขใน `llm/config.py`:
```python
SYSTEM_HINT = """
คำแนะนำใหม่สำหรับ AI...
"""
```

---

## 👥 ผู้พัฒนา

- **CWT Development Team**
- Repository: [weerachai8266/CWT-monitor_show](https://github.com/weerachai8266/CWT-monitor_show)

---

## 📄 License

Copyright © 2025 Chaiwattana Manufacturing Solutions

---

## 📞 ติดต่อ & สนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. เปิด Issue ใน GitHub Repository
2. ติดต่อทีมพัฒนา
3. ดู logs ในโฟลเดอร์ `llm/llm.log`

---

**🎉 ขอให้ใช้งานระบบอย่างมีประสิทธิภาพ!**
- `script/script.js` - JavaScript จัดการ Toggle Switch

### Backend
- `api/data.class.php` - ฟังก์ชันคำนวณเปอร์เซ็นต์และเวลาพักเบรค
- `api/get_report_data.php` - API endpoint รองรับ display_type

### Management
- `break_management.php` - หน้าจัดการเวลาพักเบรค
- `sql/create_break_times_table.sql` - SQL สำหรับสร้างตาราง

### Testing
- `test_toggle.html` - ไฟล์ทดสอบ Toggle Switch (ทำงานได้ดี ✅)

## 🎨 UI Components

### Toggle Switch
```html
<div class="display-toggle-container">
    <label class="form-label small mb-1">แสดงผล</label>
    <div class="toggle-switch">
        <input type="checkbox" id="displayToggle" class="toggle-input">
        <label for="displayToggle" class="toggle-label">
            <span class="toggle-text" data-on="เปอร์เซ็น" data-off="ชิ้น"></span>
            <span class="toggle-slider"></span>
        </label>
    </div>
</div>
```

### Summary Cards พร้อม Percentage Badge
```html
<div class="border rounded p-3 position-relative summary-card" data-line="fc">
    <h5 class="text-success" id="totalFC">0</h5>
    <small id="labelFC">F/C ชิ้น</small>
    <span class="percentage-badge d-none" id="percentageFC"></span>
</div>
```

## 📊 การทำงานของระบบ

### เมื่อสลับเป็นโหมด "เปอร์เซ็น"
1. กราฟจะเปลี่ยนสีตามเปอร์เซ็นต์
2. Summary Statistics จะแสดงเปอร์เซ็นต์แทนจำนวนชิ้น
3. Percentage Badge จะแสดงจำนวนชิ้นจริง
4. Tooltip จะแสดงสถานะ (เกินเป้าหมาย/ตามเป้าหมาย/ฯลฯ)

### เมื่อสลับเป็นโหมด "ชิ้น"
1. กราฟจะใช้สีเดิมตาม Line
2. Summary Statistics จะแสดงจำนวนชิ้นจริง
3. Percentage Badge จะซ่อน

## 🔄 Real-time Updates
- ระบบรองรับการอัปเดตแบบ Real-time ทุก 30 วินาที
- เมื่อสลับ Toggle Switch จะอัปเดตข้อมูลทันที
- Animation Effects สำหรับค่าที่ต่ำหรือสูงเกินไป

## 🧪 การทดสอบ
### ✅ ผลการทดสอบ
- **test_toggle.html**: ทำงานได้ดี - Toggle Switch สลับโหมดได้ถูกต้อง
- **Color Coding**: เปลี่ยนสีตามเปอร์เซ็นต์ได้ถูกต้อง
- **Chart Updates**: กราฟอัปเดตทันทีเมื่อสลับโหมด
- **Y-axis Labels**: แสดง % สำหรับโหมดเปอร์เซ็นต์ได้ถูกต้อง

### 🔧 การแก้ไขปัญหา
หากกราฟไม่เปลี่ยนเป็นเปอร์เซ็น:
1. ตรวจสอบ Console ว่ามี JavaScript Error หรือไม่
2. ตรวจสอบว่าตาราง `break_times` และ `sewing_target` มีข้อมูลหรือไม่
3. ลองทดสอบด้วย `test_toggle.html` ก่อน

## 🎯 เป้าหมายเริ่มต้น (ถ้าไม่มีในฐานข้อมูล)
- F/C: 160 ชิ้น/ชั่วโมง
- F/B: 135 ชิ้น/ชั่วโมง  
- R/C: 195 ชิ้น/ชั่วโมง
- R/B: 106 ชิ้น/ชั่วโมง
- 3RD: 93 ชิ้น/ชั่วโมง
- Sub: 101 ชิ้น/ชั่วโมง

## ⏰ เวลาพักเบรคเริ่มต้น
- เบรคเช้า: 08:00-08:10 (10 นาที)
- เบรคสาย: 10:00-10:10 (10 นาที)
- พักกลางวัน: 12:10-13:10 (60 นาที)
- เบรคบ่าย: 15:00-15:10 (10 นาที)
- เบรคเย็น: 16:50-17:00 (10 นาที)
- ล้างมือ: 17:00-17:30 (30 นาที)

**รวมเวลาพักเบรค: 130 นาที (2 ชั่วโมง 10 นาที)**